[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.mech"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
startCommand = "python mech_bot_main.py"

[env]
ENVIRONMENT = "production"
LOG_LEVEL = "INFO"
